{
    "_doc": "topk_output flashinfer/CSR approxTopK variant (approx_topk.cu). Selected when ``approx=True`` is passed to find(); substitutions baked from the legacy generate_approx_topk_impl codegen — find() may override __TOLERATE_RATIO__ per call via the `tolerate_ratio` kwarg.",
    "file": "kernel.cu",
    "substitutions": {
        "__THREADS_PER_BLOCK__": 1024,
        "__VORTEX_MAX_TOPK__": 2048,
        "__TOLERATE_RATIO__": 0.0
    },
    "extra_cuda_cflags": []
}
